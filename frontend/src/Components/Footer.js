/*function Footer({ isPrint = false, date }) {
  return (
    <div
      className="footer"
      style={{
        marginTop: "40px",
        marginBottom: "40px",
        border: "2px solid #000",
        borderRadius: "20px",
        padding: "10px",
        display: "flex",
        justifyContent: "space-between",
        fontSize: isPrint ? "14px" : "16px", 
        fontWeight: "500"
      }}
    >

      <div style={{ textAlign: "left" }}>
        <p><b>للتواصل والاستفسار:</b></p>
        <p>+249XXXXXXXXX</p>
        <p><b>تاريخ الفحص:</b> {date || "—"}</p>
      </div>

      <div style={{ textAlign: "right" }}>
        <p><b>موقعنا:</b> موقع المعمل</p>
        <p style={{ fontStyle: "italic" }}>
          الدقة والثقة في نتائجنا
        </p>
      </div>
    </div>
  );
}

export default Footer; */